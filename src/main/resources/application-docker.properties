spring.application.name=dowjonesindex

server.port=8080

# MySQL (docker-compose)
spring.datasource.url=jdbc:mysql://mysql:3306/StockIndexData?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=manasjena
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Actuator + Prometheus
management.endpoints.web.exposure.include=health,info,metrics,prometheus,beans,env
management.endpoint.health.show-details=always
management.endpoint.prometheus.enabled=true

`